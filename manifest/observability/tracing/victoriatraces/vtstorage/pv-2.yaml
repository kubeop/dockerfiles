apiVersion: v1
kind: PersistentVolume
metadata:
  name: vtstorage-2
  namespace: tracing
spec:
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - worker-003
  capacity:
    storage: 100Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: vtstorage
  local:
    path: /data/volume/vtstorage-2